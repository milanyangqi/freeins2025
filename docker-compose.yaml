version: '3.3'

services:
  web:
    build:
      context: ./freeins-website
      dockerfile: Dockerfile
    ports:
      - "1899:1899"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
